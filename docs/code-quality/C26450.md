---
title: C26450
keywords: C26450
author: mikeblome
ms.author: mblome
ms.date: 01/08/2017
ms.topic: reference
f1_keywords:
- C26450
helpviewer_keywords:
- C26450
dev_langs:
- C++
manager: jillfra
ms.openlocfilehash: c050173a999a8706eac7e123eee942116f59690c
ms.sourcegitcommit: 94b3a052fb1229c7e7f8804b09c1d403385c7630
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/23/2019
ms.locfileid: "62560672"
---
# <a name="arithmetic-overflow-operator-operation-causes-overflow-at-compile-time-use-a-wider-type-to-store-the-operands"></a>Aritmetické přetečení: '% – operátor %' operace způsobí přetečení v době kompilace. Pro uložení operandů použijte širší typ

 Toto upozornění signalizuje, že byl aritmetické operace prokazatelně míru ztrát v době kompilace. Můžete být uplatněna při operandy jsou všechny konstanty z doby kompilace.  V současné době zkontrolujeme operátor posunu vlevo, násobení, sčítání a odčítání operace takové přetečení.

Poznámka: C4307 je podobné kontrolu v kompilátoru C++ společnosti Microsoft.

## <a name="example-1"></a>Příklad 1

```cpp
int multiply()
{
    const int a = INT_MAX;
    const int b = 2;
    int c = a * b; // C26450 reported here
    return c;
}
```

 Chcete-li opravit toto upozornění, použijte následující kód.

```cpp
long long multiply()
{
    const int a = INT_MAX;
    const int b = 2;
    long long c = (long long)a * b; // OK
    return c;
}
```

## <a name="example-2"></a>Příklad 2

```cpp
int add()
{
    const int a = INT_MAX;
    const int b = 2;
    int c = a + b; // C26450 reported here
    return c;
}
```

 Chcete-li opravit toto upozornění, použijte následující kód:

```cpp
long long add()
{
    const int a = INT_MAX;
    const int b = 2;
    long long c = (long long)a + b; // OK
    return c;
}
```

## <a name="example-3"></a>Příklad 3

```cpp
int subtract()
{
    const int a = -INT_MAX;
    const int b = 2;
    int c = a - b; // C26450 reported here
    return c;
}
```

 Chcete-li opravit toto upozornění, použijte následující kód.

```cpp
long long subtract()
{
    const int a = -INT_MAX;
    const int b = 2;
    long long c = (long long)a - b; // OK
    return c;
}
```

## <a name="see-also"></a>Viz také
[ES.103: Není přetečení](https://github.com/isocpp/CppCoreGuidelines/blob/master/CppCoreGuidelines.md#Res-overflow)
